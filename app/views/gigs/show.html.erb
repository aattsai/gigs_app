<div class="ui vertical stripe segment">
  <div class="ui left aligned grid container performer-grid">
    <div class="ten wide column">
      <h1><%= @gig.title %></h1>
      <h2><%= @gig.location %></h2>
      <h3>$<%= @gig.price %></h3>
      <p><%= simple_format(@gig.description) %></p>
    </div>
    <div class="four wide coltrane column">
      <h4>Ads Go HERE</h4>
    </div>
  </div>
</div>